enum PopupAction {
  dismiss, // User dismisses the popup
  learnMore, // User chooses to learn more about the new features
  rateApp, // User goes to rate the app
  provideFeedback, // User chooses to provide feedback
  installWidgets // User decides to install widgets
}