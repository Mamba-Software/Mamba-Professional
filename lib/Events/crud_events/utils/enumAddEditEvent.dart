enum EditEventType {
  title, // Default
  description,
  location,
  bonos,
  startDate,
  time,
  duration,
  trainers,
  clients,
  maxMembers,
  recurrent,
  dayFromRecurrent,
  valueRecurrent,
}
