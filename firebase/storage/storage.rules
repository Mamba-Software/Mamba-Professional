rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;  // Authenticated users can create and update
      allow delete: if request.auth != null; // Authenticated users can delete
    }
  }
}
