name: mamba_castelldefels
description: MambaCastelldefels

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 0.0.13+13
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: "^0.17.0"
  flutter_rounded_date_picker: ^2.0.2
  firebase_core_platform_interface: 4.5.1
  firebase_core: ^1.19.1
  firebase_auth: "^3.0.2"
  firebase_storage: "^10.0.3"
  cloud_firestore: ^3.1.4
  provider: "^5.0.0"
  flutter_spinkit: "^5.0.0"
  http: ^0.13.3
  image_picker: ^0.8.4+11
  google_place: ^0.4.3
  uuid: ^3.0.4
  geolocator: ^8.0.0
  syncfusion_flutter_calendar: ^19.4.50
  top_snackbar_flutter: ^1.0.1
  weekday_selector: ^1.0.0
  page_transition: ^2.0.4
  flutter_launcher_icons: ^0.9.2
  preload_page_view: ^0.1.6
  cached_network_image: ^3.2.0
  flutter_native_splash: ^1.3.2
  firebase_messaging: ^11.2.4
  flutter_local_notifications: ^9.1.5
  rename: ^2.0.1
  url_launcher: ^6.0.17
  auto_size_text: ^3.0.0
  flutter_firebase_chat_core: ^1.5.1
  flutter_chat_ui: ^1.5.3
  file_picker: ^4.5.1
  mime: ^1.0.1
  resize: ^1.0.0
  carousel_slider: ^4.0.0
  flutter_image_compress: ^1.1.0
  firebase_dynamic_links: ^4.0.6
  share_plus: ^4.0.10+1
  store_redirect: ^2.0.1
  firebase_crashlytics: ^2.5.3
  firebase_analytics: ^9.1.2
  shimmer: ^2.0.0
  lazy_load_scrollview: 1.3.0
  google_maps_flutter: ^2.1.12
  maps_launcher: ^2.0.1
  sticky_headers: ^0.2.0
  flutter_rating_bar: ^4.0.0
  flutter_native_timezone: ^2.0.0
  notification_permissions: ^0.6.1
  rxdart: ^0.27.4
  flutter_speed_dial: ^6.0.0
  external_app_launcher: ^3.1.0
  qr_flutter: ^4.0.0
  qr_code_scanner: ^1.0.1
  pie_chart: ^5.3.2
  dotted_border: ^2.0.0+2
  animated_widgets: ^1.1.0
  mixpanel_flutter: ^2.0.0
  extended_nested_scroll_view: ^5.1.3
  font_awesome_flutter: ^10.2.1
  jiffy: ^5.0.0
  package_info_plus: ^1.0.2
  page_view_dot_indicator: ^2.1.0
  google_sign_in: ^5.2.1
  percent_indicator: ^4.0.1
  animated_snack_bar: ^0.3.0

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  graphic: ^0.9.2
  syncfusion_flutter_datepicker: ^19.4.56
  flutter_colorpicker: ^1.0.3
  syncfusion_flutter_charts: ^19.4.56
  flutter_bloc: ^7.1.0
  dropdown_button2: ^1.0.0
  geoflutterfire2: ^2.3.12
  purchases_flutter: ^4.11.1

flutter_icons:
  # Platforms
  android: true
  ios: false
  #  Images Path
  image_path_ios: "assets/images/launcher_icons/logoIcon.jpg"
  image_path_android: "assets/images/launcher_icons/logoIcon.jpg"
  adaptive_icon_background: "assets/images/launcher_icons/logo_background.png"
  adaptive_icon_foreground: "assets/images/launcher_icons/logo_foreground.png"

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

# run: flutter clean && flutter pub get && flutter pub run flutter_native_splash:create
flutter_native_splash:
    background_image: assets/images/launcher_icons/splash_full.png
    android: true
    ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  generate: true
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
     - assets/images/
     - assets/animations/
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Raleway
      fonts:
        - asset: assets/fonts/Raleway-Regular.ttf
        - asset: assets/fonts/Raleway-Bold.ttf
          weight: 700
    - family: Summit
      fonts:
        - asset: assets/fonts/SummitRegular-Display.ttf
          weight: 700
    - family: Futura
      fonts:
        - asset: assets/fonts/FuturaExtraBlackFont.ttf
          weight: 700
    - family: UberMove
      fonts:
        - asset: assets/fonts/UberMoveTextBold.otf
          weight: 700
        - asset: assets/fonts/UberMoveTextRegular.otf


  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
